---
título: "Asistentes de lista"
API: "GET /user/assistants/get"
descripción: "Lista de todos los asistentes del usuario autenticado con paginación"
---

Este punto de conexión le permite recuperar todos los asistentes de IA que pertenecen al usuario autenticado.

### Parámetros de consulta

<ParamField query="per_page" type="integer" optional>
  Número de asistentes por página (1-100, valor predeterminado: 10)
</ParamField>

<ParamField query="page" type="integer" optional>
  Número de página (valor predeterminado: 1)
</ParamField>

### Campos de respuesta

<ResponseField name="data" type="array">
  <Expandable title="properties">
    <ResponseField name="id" type="integer">
      El identificador único del asistente
    </ResponseField>

    <ResponseField name="user_id" type="integer">
       El ID del usuario propietario de este asistente.
    </ResponseField>

    <ResponseField name="phone_number_id" type="integer">
       El ID del número de teléfono asignado al asistente.
    </ResponseField>

    <ResponseField name="engine_id" type="integer">
      ID del motor.
    </ResponseField>

    <ResponseField name="synthesizer_id" type="integer">
      ID del sintetizador.
    </ResponseField>

    <ResponseField name="transcriber_id" type="integer">
       ID del transcriptor.
    </ResponseField>

    <ResponseField name="voice_id" type="integer">
      El ID de la voz utilizada por el asistente.
    </ResponseField>

    <ResponseField name="instance_id" type="integer">
      ID de instancia del asistente
    </ResponseField>

    <ResponseField name="name" type="string">
       Nombre del asistente
    </ResponseField>

    <ResponseField name="variables" type="object">
      Variables personalizadas definidas para el asistente
    </ResponseField>

    <ResponseField name="post_call_evaluation" type="boolean">
       Si la evaluación posterior a la llamada está habilitada.
    </ResponseField>

    <ResponseField name="fillers" type="integer">
      Si el audio de relleno está habilitado (1 = habilitado, 0 = deshabilitado).
    </ResponseField>

    <ResponseField name="post_call_schema" type="array">
      Definición del esquema para la extracción de datos posteriores a la llamada.
      <Expandable title="propiedades del esquema posterior a la llamada">
        <ResponseField name="name" type="string">
          El nombre del campo del esquema.
        </ResponseField>
        <ResponseField name="type" type="string">
          El tipo de datos (cadena, número, booleano).
        </ResponseField>
        <ResponseField name="description" type="string">
          Descripción de lo que representa este campo.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="tools" type="array">
      Matriz de herramientas disponibles para el asistente.
    </ResponseField>

    <ResponseField name="is_webhook_active" type="boolean">
      Si las notificaciones webhook están habilitadas
    </ResponseField>

    <ResponseField name="webhook_url" type="string">
      La URL webhook para notificaciones posteriores a la llamada
    </ResponseField>

    <ResponseField name="inbound_webhook_url" type="string">
      La URL del webhook para las notificaciones de llamadas entrantes.
    </ResponseField>

    <ResponseField name="language" type="string">
      Idioma.
    </ResponseField>

    <ResponseField name="type" type="string">
      El tipo de asistente (entrante o saliente).
    </ResponseField>

    <ResponseField name="status" type="string">
      El estado actual del asistente (activo o inactivo).
    </ResponseField>

    <ResponseField name="max_duration" type="integer">
      Duración máxima de la llamada en segundos.
    </ResponseField>

    <ResponseField name="record" type="boolean">
       Si se deben grabar las llamadas
    </ResponseField>

    <ResponseField name="initial_message" type="string">
      El mensaje inicial que pronunciará el asistente
    </ResponseField>

    <ResponseField name="system_prompt" type="string">
       La indicación del sistema que define el comportamiento del asistente
    </ResponseField>

    <ResponseField name="flows_platform_id" type="integer">
      ID para la integración de la plataforma de flujos
    </ResponseField>

    <ResponseField name="timezone" type="string">
      Configuración de la zona horaria para el asistente
    </ResponseField>

    <ResponseField name="created_at" type="string">
      Fecha y hora en que se creó el asistente.
    </ResponseField>

    <ResponseField name="updated_at" type="string">
      Fecha y hora en que se actualizó por última vez el asistente.
    </ResponseField>

    <ResponseField name="max_silence_duration" type="integer">
      Duración máxima del silencio en segundos antes de volver a interactuar.
    </ResponseField>

    <ResponseField name="reengagement_interval" type="integer">
      Intervalo de reinteracción en segundos.
    </ResponseField>

    <ResponseField name="deleted_at" type="string">
      Marca de tiempo de eliminación temporal (nula si no se ha eliminado).
    </ResponseField>

    <ResponseField name="end_call_on_voicemail" type="integer">
      Si se debe finalizar la llamada cuando se detecta un buzón de voz (1 = sí, 0 = no).
    </ResponseField>

    <ResponseField name="llm_temperature" type="string">
      
  Configuración de la temperatura LLM como cadena
    </ResponseField>

    <ResponseField name="voice_stability" type="string">
      Configuración de la estabilidad de la voz como cadena
    </ResponseField>

    <ResponseField name="voice_similarity" type="string">
      Configuración de la similitud de la voz como cadena
    </ResponseField>

    <ResponseField name="allow_interruptions" type="boolean">
      Si se permiten las interrupciones de la persona que llama.
    </ResponseField>

    <ResponseField name="enable_noise_cancellation" type="boolean">
      Si la cancelación de ruido está habilitada.
    </ResponseField>

    <ResponseField name="endpoint_sensitivity" type="number">
      Nivel de sensibilidad del punto final.
    </ResponseField>

    <ResponseField name="speech_speed" type="string">
       Multiplicador de velocidad del habla como cadena.
    </ResponseField>

    <ResponseField name="endpoint_type" type="string">
      Tipo de detección de actividad de voz (vad o ai).
    </ResponseField>

    <ResponseField name="wait_for_customer" type="boolean">
      Si se debe esperar a que el cliente hable primero
    </ResponseField>

    <ResponseField name="mode" type="string">
      El modo del motor (pipeline o multimodal)
    </ResponseField>

    <ResponseField name="language_id" type="integer">
       ID del idioma utilizado por el asistente
    </ResponseField>

    <ResponseField name="transcriber_provider_id" type="integer">
       ID del proveedor de transcripción
    </ResponseField>

    <ResponseField name="synthesizer_provider_id" type="integer">
      ID del proveedor del sintetizador
    </ResponseField>

    <ResponseField name="llm_model_id" type="integer">
      ID del modelo LLM utilizado
    </ResponseField>

    <ResponseField name="multimodal_model_id" type="integer">
        ID del modelo multimodal utilizado
    </ResponseField>

    <ResponseField name="ambient_sound" type="string">
       Configuración del sonido ambiental
    </ResponseField>

    <ResponseField name="uuid" type="string">
       UUID único para el asistente
    </ResponseField>

    <ResponseField name="send_webhook_only_on_completed" type="boolean">
      Si se deben enviar webhooks solo en llamadas completadas.
    </ResponseField>

    <ResponseField name="include_recording_in_webhook" type="boolean">
      Si se debe incluir la URL de la grabación en la carga útil del webhook.
    </ResponseField>

    <ResponseField name="interrupt_sensitivity" type="number">
      Nivel de sensibilidad de interrupción.
    </ResponseField>

    <ResponseField name="filler_config" type="object">
      Configuración de las respuestas de audio de relleno.
      <Expandable title="filler_config properties">
        <ResponseField name="neutral" type="array">
          Matriz de respuestas de relleno neutras.
        </ResponseField>
        <ResponseField name="negative" type="array">
          Matriz de respuestas de relleno negativas
        </ResponseField>
        <ResponseField name="positive" type="array">
          
      Matriz de respuestas de relleno positivas
        </ResponseField>
        <ResponseField name="question" type="array">
          Matriz de respuestas de relleno con preguntas
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="knowledgebase_id" type="integer">
       ID de la base de conocimientos asociada
    </ResponseField>

    <ResponseField name="knowledgebase_mode" type="string">
      Configuración del modo de la base de conocimientos
    </ResponseField>

    <ResponseField name="min_interrupt_words" type="integer">
      Número mínimo de palabras antes de que se permita la interrupción
    </ResponseField>

    <ResponseField name="ambient_sound_volume" type="string">
      Nivel de volumen del sonido ambiental como cadena
    </ResponseField>

    <ResponseField name="widget_settings" type="object">
      Configuración para la integración del widget web
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="current_page" type="integer">
  Número de página actual
</ResponseField>

<ResponseField name="per_page" type="integer">
  Número de elementos por página
</ResponseField>

<ResponseField name="total" type="integer">
  Número total de asistentes
</ResponseField>

<ResponseField name="last_page" type="integer">
  Número de página anterior
</ResponseField>

<ResponseExample>

```json 200 Response
{
  "current_page": 1,
  "data": [
    {
      "id": 127,
      "user_id": 1,
      "phone_number_id": 45,
      "engine_id": null,
      "synthesizer_id": null,
      "transcriber_id": null,
      "voice_id": 8,
      "instance_id": 1,
      "name": "Asistente de ventas",
      "variables": {
        "company_name": "Tu empresa",
        "product_line": "Servicios premium",
        "rep_name": "Asistente"
      },
      "post_call_evaluation": true,
      "fillers": 1,
      "post_call_schema": [
        {
          "name": "interest_level",
          "type": "string",
          "description": "Nivel de interés del cliente (alto, medio, bajo)"
        },
        {
          "name": "presupuesto_calificado",
          "type": "bool",
          "description": "Si el cliente potencial tiene un presupuesto adecuado"
        },
        {
          "name": "follow_up_date",
          "type": "string",
          "description": "Fecha preferida para el contacto de seguimiento"
        }
      ],
      "tools": [],
      "is_webhook_active": true,
      "webhook_url": "https://yourcompany.com/api/webhooks/sales-calls",
      "inbound_webhook_url": null,
      "language": null,
      "type": "outbound",
      "status": "active",
      "max_duration": 900,
      "record": true,
      "initial_message": "Hola, soy un asistente de Su empresa. Espero que sea un buen momento para hablar con usted. ¿Cómo está hoy?",
      "system_prompt": "Usted es representante de ventas de Su empresa. Sea profesional, amable y céntrese en calificar clientes potenciales para servicios premium.",
      "flows_platform_id": null,
      "timezone": "America/Los_Angeles",
      "created_at": "2025-07-15T14:32:15.000000Z",
      "updated_at": "2025-08-02T09:18:42.000000Z",
      "max_silence_duration": 25,
      "reengagement_interval": 45,
      "deleted_at": null,
      "end_call_on_voicemail": 1,
      "llm_temperature": "0.35",
      "voice_stability": "0.75",
      "voice_similarity": "0.85",
      "allow_interruptions": true,
      "enable_noise_cancellation": true,
      "endpoint_sensitivity": 1.8,
      "speech_speed": "1.10",
      "endpoint_type": "vad",
      "wait_for_customer": true,
      "mode": "pipeline",
      "language_id": 1,
      "transcriber_provider_id": null,
      "synthesizer_provider_id": null,
      "llm_model_id": 3,
      "multimodal_model_id": null,
      "ambient_sound": "office",
      "uuid": "a7b3c942-5f1e-4d28-8c59-2e4f7a8b9c3d",
      "send_webhook_only_on_completed": true,
      "include_recording_in_webhook": true,
      "interrupt_sensitivity": 1.2,
      "filler_config": {
        "neutral": [
          "Entiendo.",
          "De acuerdo.",
          "De acuerdo.",
          "Entendido.",
          "Vale."
        ],
        "negativo": [
          "Lo entiendo.",
          "Hmm.",
          "Entiendo.",
          "Vale."
        ],
        "positivo": [
          "Excelente!",
          "Genial!",
          "Maravilloso!",
          "Perfecto!"
        ],
        "pregunta": [
          "Déjame pensar...",
          "Buena pregunta.",
          "Hmm.",
          "Correcto."
        ]
      },
      "knowledgebase_id": 12,
      "knowledgebase_mode": "hybrid",
      "min_interrupt_words": 3,
      "ambient_sound_volume": "0.30",
      "widget_settings": {
        "theme": "modern",
        "color": "#2563eb",
        "position": "bottom-right"
      }
    }
  ],
  "first_page_url": "https://app.conecta2.ai/api/user/assistants/get?page=1",
  "from": 1,
  "last_page": 5,
  "last_page_url": "https://app.conecta2.ai/api/user/assistants/get?page=5",
  "links": [
    {
      "url": null,
      "label": "&laquo; Anterior",
      "active": false
    },
    {
      "url": "https://app.conecta2.ai/api/user/assistants/get?page=1",
      "label": "1",
      "active": true
    },
    {
      "url": "https://app.conecta2.ai/api/user/assistants/get?page=2",
      "label": "2",
      "active": false
    }
  ],
  "next_page_url": "https://app.conecta2.ai/api/user/assistants/get?page=2",
  "path": "https://app.conecta2.ai/api/user/assistants/get",
  "per_page": 10,
  "prev_page_url": null,
  "to": 10,
  "total": 47
}
```

</ResponseExample>
